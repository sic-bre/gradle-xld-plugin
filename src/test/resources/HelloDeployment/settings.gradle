pluginManagement {
    if(gradle.pluginManagmentRepos && gradle.pluginManagmentRepos.exists()) {
        apply from: gradle.pluginManagmentRepos, to: pluginManagement
    }
}

rootProject.name = 'HelloDeployment'
